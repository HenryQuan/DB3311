#!/usr/bin/php
<?php

// include the common PHP code file
require("a2.php");

$usage = "Usage: $argv[0] Name Num1 Num2";
// Init connection with database
//$db = dbConnect(DB_CONNECTION);

// Check arguments
if (count($argv) < 3) exit("$usage\n");

$name = $argv[1];
$num1 = $argv[2];
$num2 = $argv[3];

"with recursive Tree(Actor, Degree) as (
    select (100) as Actor, 0 as Degree -- pass in starting actor id and 0 as the default degree
union all
    select A.actor_id, T.Degree + 1 as Degree from Acting as A, Tree as T
    where A.movie_id in (select A.movie_id from Acting as A
        where A.actor_id = 100)
    and T.Degree < 7 -- six degrees for now
)
select * from Tree";

// Get all connections between 
?>
